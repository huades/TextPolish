# TextPolish 使用说明

## 快捷操作

在普通 `input`、`textarea` 或可编辑区域中输入文本：

- 快速连按两次空格：优化中文。
- 快速连按三次空格：翻译到配置的目标语言。

处理期间页面右下角会显示状态，失败时保留原内容并展示具体错误。

## 多线路策略

扩展按配置顺序尝试线路。每条线路可填写多个模型，模型间用逗号分隔。自动协议模式会先根据 URL 选择最可能的协议，仅在路径或数据格式不兼容时尝试其他协议；鉴权失败、限流或服务端错误不会触发无意义的协议轮询。

## 隐私与安全

- API Key 保存在 Chrome 扩展同步存储中，并只由扩展后台请求使用。
- 配置导出文件包含明文 API Key，备份后请妥善保管。
- 不要把导出的 `textpolish-*.json` 文件提交到版本库。

## Failed to fetch

新版会将这类错误显示为“网络请求未能发出”，并附带请求地址和检查建议。常见原因包括：

1. 接口地址拼写错误或服务不可达。
2. 本机代理、防火墙或 DNS 问题。
3. HTTPS 证书无效。
4. 服务端拒绝来自扩展的访问。
5. 旧版本同时发送多套鉴权头，被网关拒绝；新版已按协议发送最小必要请求头。
